 
* "target-architecture.md": "Target architecture for VB6 to ASP.NET Core Blazor Server transformation using ASP.NET Core 10.0 with Blazor Server and Entity Framework Core 10.0 with SQLite provider to preserve Orders.db; UI mapping includes VB6 MDI Container (frmMain.frm) to Blazor MainLayout.razor with NavMenu component, VB6 Child Forms to Blazor page components under /Pages, VB6 MSFlexGrid to HTML table with Blazor data binding, VB6 Toolbar/ImageList to Blazor NavMenu with icons, and VB6 ADODC data-bound controls to Blazor @bind with EF Core backing; data layer replaces ADO/ADODC with Entity Framework Core using DbContext with entity models for each table in Orders.db, all queries must use async/await (ToListAsync, FirstOrDefaultAsync), and IDbContextFactory for Blazor Server thread safety; authentication converts frmLogin.frm to ASP.NET Core Identity with cookie authentication"
* "coding-standards.md": "Company C# coding standards for VB6 to C# transformation including naming conventions (VB6 Hungarian notation to PascalCase, underscore prefix for private fields, Async suffix for async methods, I prefix for interfaces), required modern C# features (file-scoped namespaces, records for DTOs, primary constructors, collection expressions, pattern matching, target-typed new, null-coalescing assignment), architecture standards (DI registration, async database operations, IAuditable implementation with audit properties, ILogger with structured logging and correlation IDs), project structure (Pages for Blazor components per VB6 form, Components for shared components, Models for entities and DTOs, Services for business logic, Data for DbContext and repositories, Extensions for domain-organized extension methods), and company business rules including customer status mapping from VB6 integer codes to strongly-typed enums, date handling with DateOnly and DateTime.ParseExact for legacy format, audit trail requirements with IAuditable interface, and error handling standards with try-catch blocks, specific exception types, and structured logging with correlation IDs"
* "migration-paterns.md": "Contains VB6 to C# transformation examples including Form_Load to Blazor OnInitializedAsync pattern with ADODB recordset migration to Entity Framework, VB6 module function to C# extension method conversion for currency formatting, and VB6 On Error GoTo error handling to C# structured exception handling with logging"