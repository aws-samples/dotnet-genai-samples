@page "/"
@inject NavigationManager NavManager

@code {
    protected override async Task OnInitializedAsync()
    {
       NavManager.NavigateTo("/reservation-agent", true);
       await base.OnInitializedAsync();
    }
}